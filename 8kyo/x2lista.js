/*Given an array of integers, return a new array with each value doubled.*/
function maps(lista){
    for (c = 0; c < lista.length; c++){
        lista[c]= lista[c]*2;
    }
    return lista;
}
